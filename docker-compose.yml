services:
  app:
    build: .
    image: diktanalyse
    ports:
      - "5000:5000"
    environment:
      - FLASK_DEBUG=True
      - MAX_WORKERS=4
    volumes:
      # Mount source code for development (hot reload)
      - ./src/diktanalyse/app.py:/app/app.py
      - ./src/diktanalyse/tasks.py:/app/tasks.py
      - ./src/diktanalyse/templates:/app/templates
      - ./src/diktanalyse/static:/app/static
    restart: unless-stopped
